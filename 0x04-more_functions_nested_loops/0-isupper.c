#include "main.h"

/**
 * _isupper - Prints if the character is uppercase
 * @c: The character
 * Return: 1 if uppercase or 0 if otherwise
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
