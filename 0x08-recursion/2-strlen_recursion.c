#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - returns the length of string
 * @s: string
 * Return: lenghth of string
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
