#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - Changes a number to an absolute value
 * Return: 0 success
 */

int _abs(int)
{
	int n; /* @n: Number to be checked */
	
	n = abs(n);
	putchar(n);
	return (0);
}
